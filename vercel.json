{
  "buildCommand": "node scripts/migrate-db.js && next build",
  "installCommand": "yarn install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "MIGRATE_ON_BUILD": "true"
  }
}
